[Terminal]  brew install --cask orbstack
         or alternatively, install Docker Desktop
[VSCode]    download Dev Container extension
[VSCode]    open the root folder (i.e., the folder containing the .devcontainer folder) in VSCode and click "reopen in container" when prompted